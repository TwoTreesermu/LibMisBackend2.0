server:
  port: 9090  # 工程端口
spring:
  datasource:  # 数据源配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/libmisdb?characterEncoding=utf8&useSSL=false
    username: root
    password: 985211
mybatis-plus:
  mapper-locations: "classpath:mapper/**/*.xml"
  configuration:
    map-underscore-to-camel-case: true