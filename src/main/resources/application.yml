server:
  port: 9090  # 工程端口
spring:
  datasource:  # 数据源配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/libmisdb?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password:
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 输出sql相关的日志